// File: common_definitions.dart

// Enum used to control the TimerWidget's state.
enum TimerOperation { start, pause, resume, reset }
enum TimerDisplayMode {
  none,
  progressBar,
  timer,
  both,
}
